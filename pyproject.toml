[project]
name = "elyria-agent-action-boundary"
version = "0.1.0"
description = "Public-safe enterprise agent action boundary sandbox for ADMIT / HOLD / REFUSE / REVALIDATE decisions."
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Samantha Revita" }
]
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
